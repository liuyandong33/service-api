<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.api.mappers.RoleMapper">

    <select id="find" parameterType="map" resultType="build.dream.common.saas.domains.OauthClientDetail">
        SELECT * from oauth_client_detail where id = #{id}
    </select>
</mapper>